# Place the SQLite amalgamation files here for Windows builds:
#   sqlite3.c
#   sqlite3.h
#
# The root CMakeLists.txt will include sqlite3.c from this directory when WIN32
# is enabled. This directory is intentionally empty in git so downstream users
# can vendor the official amalgamation without modifying project sources.
