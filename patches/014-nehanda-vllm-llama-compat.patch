diff --git a/src/server/agent_bridge.c b/src/server/agent_bridge.c
index 73d00cbd..fad13ca4 100644
--- a/src/server/agent_bridge.c
+++ b/src/server/agent_bridge.c
@@ -41,7 +41,8 @@ static int agent_is_local_llama_compat(const agent_t *agent)
        strcmp(agent->provider, "llama-eval") == 0)
       return 1;
    return str_contains_ci(agent->name, "llama") || str_contains_ci(agent->model, "qwen") ||
-          str_contains_ci(agent->model, ".gguf");
+          str_contains_ci(agent->model, ".gguf") ||
+          str_contains_ci(agent->model, "nehanda-rag-synthesis");
 }
 static int agent_is_mistral_vibe_model(const agent_t *agent)
 {
