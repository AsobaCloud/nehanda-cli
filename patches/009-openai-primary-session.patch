diff --git a/src/server/agent_adapter.c b/src/server/agent_adapter.c
index ec8a57f5..7e623657 100644
--- a/src/server/agent_adapter.c
+++ b/src/server/agent_adapter.c
@@ -49,6 +49,19 @@ static const agent_adapter_t g_adapters[] = {
         .capabilities = AGENT_ADAPTER_CAP_DELEGATE_TURN | AGENT_ADAPTER_CAP_PRIMARY_CONVERSATION |
                         AGENT_ADAPTER_CAP_DIRECT_HTTP | AGENT_ADAPTER_CAP_PRIMARY_SESSION,
     },
+    /* OpenAI-compatible HTTP (Nehanda vLLM, OpenAI API, Ollama /v1, etc.).
+     * Without PRIMARY_SESSION, chat_stream_worker routes these agents to the
+     * one-shot agent path and each turn loses prior conversation history. */
+    {
+        .name = "openai",
+        .provider = "openai",
+        .display_name = "OpenAI-compatible",
+        .default_endpoint = "https://api.openai.com/v1",
+        .default_model = "gpt-4o",
+        .auth_type = "bearer",
+        .capabilities = AGENT_ADAPTER_CAP_DELEGATE_TURN | AGENT_ADAPTER_CAP_PRIMARY_CONVERSATION |
+                        AGENT_ADAPTER_CAP_DIRECT_HTTP | AGENT_ADAPTER_CAP_PRIMARY_SESSION,
+    },
     {0},
 };
 
