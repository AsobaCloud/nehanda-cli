diff --git a/src/Makefile b/src/Makefile
index f6d7ec80..57d2f9e1 100644
--- a/src/Makefile
+++ b/src/Makefile
@@ -90,7 +90,7 @@ endif
 # would fail trying to write .d dependency files into non-existent subdirectories.
 _DUMMY := $(shell mkdir -p $(OBJDIR) $(OBJDIR)/posix $(OBJDIR)/linux $(OBJDIR)/mac $(OBJDIR)/windows $(OBJDIR)/tests $(OBJDIR)/db1 $(OBJDIR)/db2)
 
-C_FLAGS = -Os -flto -ffunction-sections -fdata-sections -Wl,--gc-sections -s -Wall -Wextra -Werror -Wno-unused-parameter -Wno-format-truncation -Wno-unused-result -MMD -MP -I. -Iheaders -Ivendor/headers -Idb1 -Idb2 -Iworkflow $(VERSION_FLAGS) $(EXTRA_C_FLAGS)
+C_FLAGS = -Os $(FLTO_FLAG) -ffunction-sections -fdata-sections $(GC_LINK_FLAG) $(STRIP_FLAG) -Wall -Wextra -Werror -Wno-unused-parameter -Wno-format-truncation -Wno-unused-result -MMD -MP -I. -Iheaders -Ivendor/headers -Idb1 -Idb2 -Iworkflow $(VERSION_FLAGS) $(EXTRA_C_FLAGS)
 
 # DB2 client flags are discovered with pkg-config because provider headers are
 # not always installed under the compiler default include path. See
@@ -108,6 +108,21 @@ endif
 
 UNAME_S := $(shell uname -s)
 
+ifeq ($(UNAME_S),Darwin)
+    GC_LINK_FLAG =
+    STRIP_FLAG   =
+    GC_LFLAGS    = -Wl,-dead_strip
+    PLATFORM_LIBS = -lutil
+    FLTO_FLAG    =
+    C_FLAGS += -Wno-error=unused-function -Wno-error=unused-but-set-variable -Wno-error=deprecated-non-prototype -Wno-error=deprecated-declarations
+else
+    GC_LINK_FLAG = -Wl,--gc-sections
+    STRIP_FLAG   = -s
+    GC_LFLAGS    = -Wl,--gc-sections
+    PLATFORM_LIBS =
+    FLTO_FLAG    = -flto
+endif
+
 # Platform-specific HTTP library
 ifeq ($(OS),Windows_NT)
     HTTP_LIB = -lwinhttp
@@ -202,8 +217,8 @@ LEAN_KB = ../aimee-kb-lean$(EXEEXT)
 LEAN_OBJDIR = build/obj-lean
 
 # Linker flag sets (layered -- each level adds to the previous)
-L_MINIMAL = -Os -flto -ffunction-sections -fdata-sections -Wl,--gc-sections -s -lpthread $(EXTRA_L_FLAGS)
-L_CORE    = -Os -flto -ffunction-sections -fdata-sections -Wl,--gc-sections -s -lsqlite3 $(PQ_LIB) -lm -lpthread -lzstd $(EXTRA_L_FLAGS)
+L_MINIMAL = -Os $(FLTO_FLAG) -ffunction-sections -fdata-sections $(GC_LFLAGS) $(STRIP_FLAG) -lpthread $(EXTRA_L_FLAGS)
+L_CORE    = -Os $(FLTO_FLAG) -ffunction-sections -fdata-sections $(GC_LFLAGS) $(STRIP_FLAG) -lsqlite3 $(PQ_LIB) -lm -lpthread -lzstd $(EXTRA_L_FLAGS)
 L_AGENT   = $(L_CORE) -lssl -lcrypto $(HTTP_LIB)
 L_FULL    = $(L_AGENT) $(PAM_LIBS) $(LIBSECRET_LIBS) $(PLATFORM_FRAMEWORKS) $(WINDOWS_LIBS)
 L_CLIENT  = $(L_MINIMAL) $(TLS_LIBS) -lm
@@ -211,8 +226,8 @@ L_GATEWAY = $(L_MINIMAL) -lssl -lcrypto $(HTTP_LIB)
 # Server (DB1 daemon) link flags — same as L_FULL minus libpq. The daemon
 # no longer carries DB2 code; the only postgres-bound work routes
 # through the kb client socket to aimee-kb, which carries libpq itself.
-L_SERVER  = -Os -flto -ffunction-sections -fdata-sections -Wl,--gc-sections -s -lsqlite3 -lm -lpthread $(EXTRA_L_FLAGS) -lssl -lcrypto $(HTTP_LIB) $(PAM_LIBS) $(LIBSECRET_LIBS) $(PLATFORM_FRAMEWORKS) $(WINDOWS_LIBS)
-L_KB      = -Os -flto -ffunction-sections -fdata-sections -Wl,--gc-sections -s $(PQ_LIB) -lm -lpthread -lzstd $(EXTRA_L_FLAGS) -lssl -lcrypto $(HTTP_LIB) $(PAM_LIBS) $(LIBSECRET_LIBS) $(PLATFORM_FRAMEWORKS) $(WINDOWS_LIBS)
+L_SERVER  = -Os $(FLTO_FLAG) -ffunction-sections -fdata-sections $(GC_LFLAGS) $(STRIP_FLAG) -lsqlite3 -lm -lpthread $(EXTRA_L_FLAGS) -lssl -lcrypto $(HTTP_LIB) $(PAM_LIBS) $(LIBSECRET_LIBS) $(PLATFORM_FRAMEWORKS) $(PLATFORM_LIBS) $(WINDOWS_LIBS)
+L_KB      = -Os $(FLTO_FLAG) -ffunction-sections -fdata-sections $(GC_LFLAGS) $(STRIP_FLAG) $(PQ_LIB) -lm -lpthread -lzstd $(EXTRA_L_FLAGS) -lssl -lcrypto $(HTTP_LIB) $(PAM_LIBS) $(LIBSECRET_LIBS) $(PLATFORM_FRAMEWORKS) $(PLATFORM_LIBS) $(WINDOWS_LIBS)
 ifeq ($(OS),Windows_NT)
     L_FULL += -ladvapi32
     L_KB += -ladvapi32
@@ -358,8 +373,16 @@ CLI_SRCS = cli_v1_routes.c cli_v1_routes_d.c cli_v1_routes_c.c cli_v1_routes_b.c
 ifeq ($(WITH_TLS),1)
 CLI_SRCS += aimee_tls.c
 TLS_OBJS = $(OBJDIR)/aimee_tls.o
+ifeq ($(UNAME_S),Darwin)
+SERVER_TLS_OBJ = $(OBJDIR)/aimee_tls_securetransport.o
+else ifeq ($(OS),Windows_NT)
+SERVER_TLS_OBJ = $(OBJDIR)/aimee_tls_schannel.o
+else
+SERVER_TLS_OBJ = $(OBJDIR)/aimee_tls.o
+endif
 else
 TLS_OBJS =
+SERVER_TLS_OBJ =
 endif
 CLI_OBJS = $(CLI_SRCS:%.c=$(OBJDIR)/%.o)
 CLIENT_PLATFORM_OBJS = $(filter %/platform_ipc.o %/platform_net.o %/platform_path.o %/platform_process.o %/history.o %/cli_client.o %/cli_main.o,$(PLATFORM_OBJS))
@@ -901,7 +924,7 @@ $(WEBCHAT): $(WEBCHAT_GO_SRCS)
 # synthesize a tcp: endpoint from a --server / AIMEE_SERVER_URL / remote.conf
 # target. --gc-sections drops the rest of aimee_client (the actual TCP/TLS
 # transport), so no extra net/tls/uds objects are needed here.
-$(SERVER): $(SERVER_OBJS) $(SERVER_KB_CLIENT_OBJS) $(AGENT_OBJS) $(SERVER_DATA_OBJS) $(SERVER_CMD_OBJS) $(CORE_OBJS) $(DB1_OBJS) $(PLATFORM_OBJS) $(MCP_GIT_OBJS) $(OBJDIR)/aimee_client.o
+$(SERVER): $(SERVER_OBJS) $(SERVER_KB_CLIENT_OBJS) $(AGENT_OBJS) $(SERVER_DATA_OBJS) $(SERVER_CMD_OBJS) $(CORE_OBJS) $(DB1_OBJS) $(PLATFORM_OBJS) $(MCP_GIT_OBJS) $(OBJDIR)/aimee_client.o $(SERVER_TLS_OBJ)
 	$(AIMEE_RUNTIME_MUTATION_GUARD)
 	$(CC) -o $@ $^ $(L_SERVER)
 
diff --git a/src/kb/http/kb_http.c b/src/kb/http/kb_http.c
index c2ce2741..1469c1fe 100644
--- a/src/kb/http/kb_http.c
+++ b/src/kb/http/kb_http.c
@@ -54,6 +54,7 @@
 #define KB_HTTP_BACKLOG  16
 #define KB_HTTP_READ_MAX 4096
 #define KB_HTTP_RESP_MAX (1024 * 1024)
+#define KB_HTTP_THREAD_STACK ((size_t)32 * 1024 * 1024)
 
 extern kb_service_ctx_t *g_kb_ctx;
 int kb_dispatch_action_json(const char *action, const char *body, int body_len, char *out_buf,
@@ -237,7 +238,15 @@ int kb_http_start(int port, const char *bearer_token)
    }
 
    g_running = 1;
-   if (pthread_create(&g_thread, NULL, listener_thread, NULL) != 0)
+   pthread_attr_t attr;
+   pthread_attr_init(&attr);
+   int started = 0;
+   if (pthread_attr_setstacksize(&attr, KB_HTTP_THREAD_STACK) == 0)
+      started = pthread_create(&g_thread, &attr, listener_thread, NULL) == 0;
+   else
+      started = pthread_create(&g_thread, NULL, listener_thread, NULL) == 0;
+   pthread_attr_destroy(&attr);
+   if (!started)
    {
       g_running = 0;
       close(g_listen_fd);
diff --git a/src/kb/kb_curator_drain.c b/src/kb/kb_curator_drain.c
index d66b8832..fc17fed5 100644
--- a/src/kb/kb_curator_drain.c
+++ b/src/kb/kb_curator_drain.c
@@ -45,6 +45,7 @@
 #include <unistd.h>
 
 #define DRAIN_POLL_SECS 5
+#define KB_BG_THREAD_STACK ((size_t)32 * 1024 * 1024)
 /* Max synthesis ops processed per poll — each is an LLM round-trip. */
 #define SYNTH_DRAIN_BATCH 4
 /* Max curator jobs (extract/synth/…) drained back-to-back per poll. The per-poll
@@ -112,8 +113,10 @@ static void *drain_thread_main(void *arg)
 {
    kb_curator_drain_ctx_t *ctx = (kb_curator_drain_ctx_t *)arg;
 
-   config_t cfg;
-   config_load(&cfg);
+   config_t *cfg = calloc(1, sizeof(*cfg));
+   if (!cfg)
+      return NULL;
+   config_load(cfg);
 
    /* Cold-start backfill: a quiescent / already-indexed corpus never produces a
     * built>0 event, so the incremental rebuild below would never fire and
@@ -147,7 +150,7 @@ static void *drain_thread_main(void *arg)
       /* Reload config periodically */
       if (now - last_cfg_reload >= 300)
       {
-         config_load(&cfg);
+         config_load(cfg);
          last_cfg_reload = now;
       }
 
@@ -156,17 +159,17 @@ static void *drain_thread_main(void *arg)
        * routes/identities exist before the structural-edge gate queries them.
        * Cleared only on a successful (0-return) rebuild, so a failed attempt stays
        * pending and retries — the one-shot guarantee is the flag-clear here. */
-      if (cross_repo_cold_start_pending && cfg.kb_curator_cross_repo_graph_enabled &&
-          kb_cross_repo_meta_rebuild(&cfg) == 0)
+      if (cross_repo_cold_start_pending && cfg->kb_curator_cross_repo_graph_enabled &&
+          kb_cross_repo_meta_rebuild(cfg) == 0)
          cross_repo_cold_start_pending = 0;
 
       /* Evidence-vector embed drain — runs every poll, independent of the
        * curator extract gates (it fills evidence_vectors for the neighbourhood
        * builder; the builtin embedder needs no external sidecar). */
-      if (cfg.kb_evidence_embed_enabled)
+      if (cfg->kb_evidence_embed_enabled)
       {
-         const char *embed_cmd = config_embedding_command(&cfg, NULL);
-         int n = kb_evidence_embed_drain(cfg.kb_evidence_embed_batch, embed_cmd);
+         const char *embed_cmd = config_embedding_command(cfg, NULL);
+         int n = kb_evidence_embed_drain(cfg->kb_evidence_embed_batch, embed_cmd);
          if (n > 0)
             aimee_log(LOG_DEBUG, "kb.evidence.embed", "drained %d evidence op(s)", n);
       }
@@ -188,9 +191,9 @@ static void *drain_thread_main(void *arg)
        * curator extract gates. Pulls "memory_facts" jobs enqueued by memory.store
        * and runs the general LLM extractor over each memory's content. Gated on
        * typed_facts_enabled (a no-op when off). */
-      if (cfg.typed_facts_enabled)
+      if (cfg->typed_facts_enabled)
       {
-         int n = kb_memory_facts_drain(&cfg, 8);
+         int n = kb_memory_facts_drain(cfg, 8);
          if (n > 0)
             aimee_log(LOG_DEBUG, "kb.memory.facts", "drained %d memory_facts job(s)", n);
       }
@@ -204,7 +207,7 @@ static void *drain_thread_main(void *arg)
        * up over a handful of polls (max_points caps each project per poll). It is
        * gated only on an embedder being configured — no external LLM, no curator
        * gate — so embeddings appear automatically right after ingest. */
-      if (cfg.embedding_command[0])
+      if (cfg->embedding_command[0])
       {
          project_info_t projects[128];
          int np = index_list_projects(projects, 128);
@@ -226,19 +229,19 @@ static void *drain_thread_main(void *arg)
        * …) into the curated kb_documents layer, reading content from DB2
        * file_contents (no disk). Bounded per poll; backfills then idles cheaply.
        * Same embedder gate as the code drain. */
-      if (cfg.embedding_command[0])
+      if (cfg->embedding_command[0])
       {
          project_info_t projects[128];
          int np = index_list_projects(projects, 128);
          int total = 0;
          for (int i = 0; i < np; i++)
          {
-            int e = kb_doc_refresh(projects[i].name, cfg.embedding_command, 200);
+            int e = kb_doc_refresh(projects[i].name, cfg->embedding_command, 200);
             if (e > 0)
                total += e;
             /* Self-heal chunks that exist but lost their embedding (partial
              * ingest, late embedder, or a vector-store dim reset). */
-            int b = kb_doc_embed_backfill(projects[i].name, cfg.embedding_command, 200);
+            int b = kb_doc_embed_backfill(projects[i].name, cfg->embedding_command, 200);
             if (b > 0)
                total += b;
          }
@@ -263,7 +266,7 @@ static void *drain_thread_main(void *arg)
        * `aimee graph sync-code`. Pure DB2 (reads files/terms/code_calls), no
        * embedder/LLM. Gated on its own flag (default on); independent of the curator
        * pipeline below. */
-      if (cfg.kb_curator_projection_graph_enabled)
+      if (cfg->kb_curator_projection_graph_enabled)
       {
          project_info_t projects[128];
          int np = index_list_projects(projects, 128);
@@ -294,28 +297,28 @@ static void *drain_thread_main(void *arg)
           * possible future optimization). The flag here also gates the resolver
           * (cross_repo_deps.c) and the cold-start backfill above: disabling it
           * turns off precision gating AND route population together. */
-         if (built > 0 && cfg.kb_curator_cross_repo_graph_enabled)
-            kb_cross_repo_meta_rebuild(&cfg);
+         if (built > 0 && cfg->kb_curator_cross_repo_graph_enabled)
+            kb_cross_repo_meta_rebuild(cfg);
       }
 
       /* Candidate-generation synthesis drain — the heavy LLM pass, on the
        * scheduler, never on the capture hot path. Off by default. */
-      if (cfg.learning_synthesize_enabled && cfg.learning_synthesize_command[0])
+      if (cfg->learning_synthesize_enabled && cfg->learning_synthesize_command[0])
       {
-         const char *embed_cmd = config_embedding_command(&cfg, NULL);
+         const char *embed_cmd = config_embedding_command(cfg, NULL);
          /* Bound LLM calls per poll — each op is one sidecar/LLM round-trip. */
          int n =
-             kb_learning_synth_drain(SYNTH_DRAIN_BATCH, cfg.learning_synthesize_command, embed_cmd,
-                                     cfg.learning_synthesize_k, cfg.learning_synthesize_max_tokens);
+             kb_learning_synth_drain(SYNTH_DRAIN_BATCH, cfg->learning_synthesize_command, embed_cmd,
+                                     cfg->learning_synthesize_k, cfg->learning_synthesize_max_tokens);
          if (n > 0)
             aimee_log(LOG_DEBUG, "kb.learning.synth", "drained %d synthesis op(s)", n);
       }
 
-      if (!cfg.kb_curator_extract_docs_enabled && !cfg.kb_curator_extract_code_enabled &&
-          !cfg.kb_curator_resolve_entities_enabled && !cfg.kb_curator_index_narrative_enabled &&
-          !cfg.kb_curator_index_claims_enabled && !cfg.kb_curator_detect_contradictions_enabled &&
-          !cfg.kb_curator_index_code_unit_enabled && !cfg.kb_curator_link_artifacts_enabled &&
-          !cfg.kb_curator_synthesize_enabled && !cfg.kb_curator_promote_entity_enabled)
+      if (!cfg->kb_curator_extract_docs_enabled && !cfg->kb_curator_extract_code_enabled &&
+          !cfg->kb_curator_resolve_entities_enabled && !cfg->kb_curator_index_narrative_enabled &&
+          !cfg->kb_curator_index_claims_enabled && !cfg->kb_curator_detect_contradictions_enabled &&
+          !cfg->kb_curator_index_code_unit_enabled && !cfg->kb_curator_link_artifacts_enabled &&
+          !cfg->kb_curator_synthesize_enabled && !cfg->kb_curator_promote_entity_enabled)
       {
          kb_background_clear("curator");
          continue;
@@ -324,10 +327,10 @@ static void *drain_thread_main(void *arg)
       kb_curator_extract_opts_t opts;
       memset(&opts, 0, sizeof(opts));
       snprintf(opts.extract_command, sizeof(opts.extract_command), "%s",
-               cfg.kb_curator_extract_command);
+               cfg->kb_curator_extract_command);
       opts.max_tokens =
-          cfg.kb_curator_extract_max_tokens > 0 ? cfg.kb_curator_extract_max_tokens : 2048;
-      opts.max_attempts = cfg.kb_curator_max_attempts > 0 ? cfg.kb_curator_max_attempts : 3;
+          cfg->kb_curator_extract_max_tokens > 0 ? cfg->kb_curator_extract_max_tokens : 2048;
+      opts.max_attempts = cfg->kb_curator_max_attempts > 0 ? cfg->kb_curator_max_attempts : 3;
 
       /* Drain a batch of curator jobs back-to-back. Each iteration runs ONE job
        * from the highest-priority non-empty stage (the rc chain below). Draining a
@@ -343,52 +346,52 @@ static void *drain_thread_main(void *arg)
           * by the next stage); cheap no-op when nothing is held. */
          db2_lease_release_idle();
          int rc = 0;
-         if (cfg.kb_curator_extract_docs_enabled)
+         if (cfg->kb_curator_extract_docs_enabled)
          {
             kb_background_set("curator", "extract docs");
             rc = kb_curator_extract_one(&opts);
          }
-         if (rc == 0 && cfg.kb_curator_extract_code_enabled)
+         if (rc == 0 && cfg->kb_curator_extract_code_enabled)
          {
             kb_background_set("curator", "extract code");
             rc = kb_curator_extract_code_unit_one(&opts);
          }
-         if (rc == 0 && cfg.kb_curator_resolve_entities_enabled)
+         if (rc == 0 && cfg->kb_curator_resolve_entities_enabled)
          {
             kb_background_set("curator", "resolve entities");
             rc = kb_curator_resolve_entities_one(&opts);
          }
-         if (rc == 0 && cfg.kb_curator_index_narrative_enabled)
+         if (rc == 0 && cfg->kb_curator_index_narrative_enabled)
          {
             kb_background_set("curator", "index narrative");
             rc = kb_curator_index_narrative_one(&opts);
          }
-         if (rc == 0 && cfg.kb_curator_index_claims_enabled)
+         if (rc == 0 && cfg->kb_curator_index_claims_enabled)
          {
             kb_background_set("curator", "index claims");
             rc = kb_curator_index_claims_one(&opts);
          }
-         if (rc == 0 && cfg.kb_curator_detect_contradictions_enabled)
+         if (rc == 0 && cfg->kb_curator_detect_contradictions_enabled)
          {
             kb_background_set("curator", "detect contradictions");
             rc = kb_curator_detect_contradictions_one(&opts);
          }
-         if (rc == 0 && cfg.kb_curator_index_code_unit_enabled)
+         if (rc == 0 && cfg->kb_curator_index_code_unit_enabled)
          {
             kb_background_set("curator", "index code_unit");
             rc = kb_curator_index_code_unit_one(&opts);
          }
-         if (rc == 0 && cfg.kb_curator_link_artifacts_enabled)
+         if (rc == 0 && cfg->kb_curator_link_artifacts_enabled)
          {
             kb_background_set("curator", "link artifacts");
             rc = kb_curator_link_artifacts_one(&opts);
          }
-         if (rc == 0 && cfg.kb_curator_synthesize_enabled)
+         if (rc == 0 && cfg->kb_curator_synthesize_enabled)
          {
             kb_background_set("curator", "synthesize topic");
             rc = kb_curator_synthesize_one(&opts);
          }
-         if (rc == 0 && cfg.kb_curator_promote_entity_enabled)
+         if (rc == 0 && cfg->kb_curator_promote_entity_enabled)
          {
             kb_background_set("curator", "promote entity");
             rc = kb_curator_promote_entity_one(&opts);
@@ -411,6 +414,7 @@ static void *drain_thread_main(void *arg)
    }
 
    kb_background_clear("curator");
+   free(cfg);
    return NULL;
 }
 
@@ -441,7 +445,15 @@ void kb_curator_drain_init(kb_curator_drain_ctx_t *ctx)
    }
 
    ctx->stop = 0;
-   if (pthread_create(&ctx->thread, NULL, drain_thread_main, ctx) == 0)
+   pthread_attr_t attr;
+   pthread_attr_init(&attr);
+   int started = 0;
+   if (pthread_attr_setstacksize(&attr, KB_BG_THREAD_STACK) == 0)
+      started = pthread_create(&ctx->thread, &attr, drain_thread_main, ctx) == 0;
+   else
+      started = pthread_create(&ctx->thread, NULL, drain_thread_main, ctx) == 0;
+   pthread_attr_destroy(&attr);
+   if (started)
    {
       ctx->active = 1;
       aimee_log(LOG_INFO, "kb.curator.drain", "drain thread started");
diff --git a/src/kb/kb_ingest_workers.c b/src/kb/kb_ingest_workers.c
index 9e5bc7fb..e948af90 100644
--- a/src/kb/kb_ingest_workers.c
+++ b/src/kb/kb_ingest_workers.c
@@ -50,6 +50,7 @@
  * default embedder is pplx-embed-v1-4b (2560-dim). Advisory only — the real
  * column dimension comes from the schema (see kbiw_process_job). */
 #define KB_DEFAULT_DIM 2560
+#define KB_INGEST_THREAD_STACK ((size_t)32 * 1024 * 1024)
 
 /* ------------------------------------------------------------------ */
 /* notify: wake parked workers (called by kb_handle_ingest on enqueue) */
@@ -70,30 +71,39 @@ void kb_worker_notify(kb_service_ctx_t *ctx)
 
 static void kbiw_enqueue_all(kb_service_ctx_t *ctx)
 {
-   config_t cfg;
-   config_load(&cfg);
-   if (!cfg.kb_bg_ingest_enabled || !db2_is_initialized())
+   config_t *cfg = calloc(1, sizeof(*cfg));
+   if (!cfg)
+      return;
+   config_load(cfg);
+   if (!cfg->kb_bg_ingest_enabled || !db2_is_initialized())
+   {
+      free(cfg);
       return;
+   }
 
    char(*projects)[MAX_PATH_LEN] = calloc(MAX_DISCOVERED_PROJECTS, MAX_PATH_LEN);
    if (!projects)
+   {
+      free(cfg);
       return;
+   }
 
    int total = 0;
-   for (int w = 0; w < cfg.workspace_count; w++)
+   for (int w = 0; w < cfg->workspace_count; w++)
    {
-      int n = workspace_discover_projects(cfg.workspaces[w], 3, projects, MAX_DISCOVERED_PROJECTS);
+      int n = workspace_discover_projects(cfg->workspaces[w], 3, projects, MAX_DISCOVERED_PROJECTS);
       for (int i = 0; i < n; i++)
       {
          char pname[256];
          char pws[256];
-         workspace_repo_index_keys(projects[i], cfg.workspaces[w], pname, sizeof(pname), pws,
+         workspace_repo_index_keys(projects[i], cfg->workspaces[w], pname, sizeof(pname), pws,
                                    sizeof(pws));
          db2_kb_ingest_queue_enqueue(pname, projects[i], pws, 0);
          total++;
       }
    }
    free(projects);
+   free(cfg);
 
    if (total > 0)
    {
@@ -124,9 +134,14 @@ static void kbiw_process_job(const db2_kb_ingest_job_t *job)
       return;
    }
 
-   config_t cfg;
-   config_load(&cfg);
-   const char *embed_cmd = config_embedding_command(&cfg, NULL);
+   config_t *cfg = calloc(1, sizeof(*cfg));
+   if (!cfg)
+   {
+      kb_background_clear("ingest");
+      return;
+   }
+   config_load(cfg);
+   const char *embed_cmd = config_embedding_command(cfg, NULL);
 
    kb_stats_t stats;
    memset(&stats, 0, sizeof(stats));
@@ -136,6 +151,7 @@ static void kbiw_process_job(const db2_kb_ingest_job_t *job)
       aimee_log(LOG_WARN, "kb.ingest.worker", "kb_build failed for project='%s'", job->project);
       db2_kb_ingest_queue_fail(job->id, "kb_build failed");
       kb_background_clear("ingest");
+      free(cfg);
       return;
    }
 
@@ -152,6 +168,7 @@ static void kbiw_process_job(const db2_kb_ingest_job_t *job)
                 job->project);
       db2_kb_ingest_queue_fail(job->id, "canonical index scan failed");
       kb_background_clear("ingest");
+      free(cfg);
       return;
    }
 
@@ -159,6 +176,7 @@ static void kbiw_process_job(const db2_kb_ingest_job_t *job)
                                 stats.embeddings_added);
    db2_kb_runtime_state_set_now("last_ingest_at");
    kb_background_clear("ingest");
+   free(cfg);
 
    aimee_log(LOG_INFO, "kb.ingest.worker", "done: project='%s' files=%d chunks=%d embeddings=%d",
              job->project, stats.files_indexed, stats.chunks_added, stats.embeddings_added);
@@ -234,11 +252,14 @@ static void *kbiw_timer_thread(void *arg)
    /* Fire once on startup. */
    kbiw_enqueue_all(ctx);
 
+   config_t *cfg = calloc(1, sizeof(*cfg));
+   if (!cfg)
+      return NULL;
+
    for (;;)
    {
-      config_t cfg;
-      config_load(&cfg);
-      int interval_secs = cfg.kb_bg_ingest_interval_hours * 3600;
+      config_load(cfg);
+      int interval_secs = cfg->kb_bg_ingest_interval_hours * 3600;
       if (interval_secs <= 0)
          interval_secs = 6 * 3600;
 
@@ -246,7 +267,10 @@ static void *kbiw_timer_thread(void *arg)
       while (slept < interval_secs)
       {
          if (ctx->ingest_stop)
+         {
+            free(cfg);
             return NULL;
+         }
          int chunk = (interval_secs - slept > 5) ? 5 : (interval_secs - slept);
 #ifndef AIMEE_WINDOWS
          sleep((unsigned int)chunk);
@@ -254,10 +278,12 @@ static void *kbiw_timer_thread(void *arg)
          slept += chunk;
       }
       if (ctx->ingest_stop)
+      {
+         free(cfg);
          return NULL;
+      }
 
-      config_load(&cfg);
-      if (cfg.kb_bg_ingest_enabled)
+      if (cfg->kb_bg_ingest_enabled)
          kbiw_enqueue_all(ctx);
    }
 }
@@ -409,7 +435,15 @@ void kb_ingest_workers_start(kb_service_ctx_t *ctx)
 
    for (int i = 0; i < cap; i++)
    {
-      if (pthread_create(&ctx->ingest_threads[i], NULL, kbiw_worker_thread, ctx) == 0)
+      pthread_attr_t wattr;
+      pthread_attr_init(&wattr);
+      int ok = 0;
+      if (pthread_attr_setstacksize(&wattr, KB_INGEST_THREAD_STACK) == 0)
+         ok = pthread_create(&ctx->ingest_threads[i], &wattr, kbiw_worker_thread, ctx) == 0;
+      else
+         ok = pthread_create(&ctx->ingest_threads[i], NULL, kbiw_worker_thread, ctx) == 0;
+      pthread_attr_destroy(&wattr);
+      if (ok)
          ctx->ingest_count++;
       else
          aimee_log(LOG_ERROR, "kb.ingest", "failed to start ingest worker %d", i);
@@ -421,7 +455,15 @@ void kb_ingest_workers_start(kb_service_ctx_t *ctx)
 
    if (cfg.kb_bg_ingest_enabled)
    {
-      if (pthread_create(&ctx->ingest_timer_thread, NULL, kbiw_timer_thread, ctx) == 0)
+      pthread_attr_t tattr;
+      pthread_attr_init(&tattr);
+      int ok = 0;
+      if (pthread_attr_setstacksize(&tattr, KB_INGEST_THREAD_STACK) == 0)
+         ok = pthread_create(&ctx->ingest_timer_thread, &tattr, kbiw_timer_thread, ctx) == 0;
+      else
+         ok = pthread_create(&ctx->ingest_timer_thread, NULL, kbiw_timer_thread, ctx) == 0;
+      pthread_attr_destroy(&tattr);
+      if (ok)
          ctx->ingest_timer_active = 1;
    }
 
diff --git a/src/kb/kb_mining.c b/src/kb/kb_mining.c
index 1f49b00a..ab0d6928 100644
--- a/src/kb/kb_mining.c
+++ b/src/kb/kb_mining.c
@@ -428,6 +428,8 @@ int kb_mining_run_once(void)
    return ran;
 }
 
+#define KB_BG_THREAD_STACK ((size_t)32 * 1024 * 1024)
+
 static void *mining_thread_main(void *arg)
 {
    int min_poll_s = *(int *)arg;
@@ -455,7 +457,15 @@ int kb_mining_start(int min_poll_s)
       return -1;
    *arg = min_poll_s;
    g_mining_stop = 0;
-   if (pthread_create(&g_mining_thread, NULL, mining_thread_main, arg) != 0)
+   pthread_attr_t attr;
+   pthread_attr_init(&attr);
+   int started = 0;
+   if (pthread_attr_setstacksize(&attr, KB_BG_THREAD_STACK) == 0)
+      started = pthread_create(&g_mining_thread, &attr, mining_thread_main, arg) == 0;
+   else
+      started = pthread_create(&g_mining_thread, NULL, mining_thread_main, arg) == 0;
+   pthread_attr_destroy(&attr);
+   if (!started)
    {
       free(arg);
       return -1;
diff --git a/src/kb/kb_reflection.c b/src/kb/kb_reflection.c
index 483d6dc9..50c821c7 100644
--- a/src/kb/kb_reflection.c
+++ b/src/kb/kb_reflection.c
@@ -33,6 +33,8 @@
 #include <time.h>
 #include <unistd.h>
 
+#define KB_BG_THREAD_STACK ((size_t)32 * 1024 * 1024)
+
 /* Declared in kb_service_workers.c */
 extern kb_service_ctx_t *g_kb_ctx;
 
@@ -260,9 +262,11 @@ static void *reflection_thread_main(void *arg)
 {
    kb_reflection_ctx_t *ctx = (kb_reflection_ctx_t *)arg;
 
-   config_t cfg;
-   config_load(&cfg);
-   long idle_threshold = (long)cfg.review_idle_trigger_minutes * 60;
+   config_t *cfg = calloc(1, sizeof(*cfg));
+   if (!cfg)
+      return NULL;
+   config_load(cfg);
+   long idle_threshold = (long)cfg->review_idle_trigger_minutes * 60;
    if (idle_threshold <= 0)
       idle_threshold = 1800;
 
@@ -284,12 +288,12 @@ static void *reflection_thread_main(void *arg)
       /* Reload config periodically for live changes */
       long now = (long)time(NULL);
       if ((now - last_fired) % 300 == 0)
-         config_load(&cfg);
+         config_load(cfg);
 
-      if (!cfg.review_scheduler_enabled)
+      if (!cfg->review_scheduler_enabled)
          continue;
 
-      idle_threshold = (long)cfg.review_idle_trigger_minutes * 60;
+      idle_threshold = (long)cfg->review_idle_trigger_minutes * 60;
       if (idle_threshold <= 0)
          idle_threshold = 1800;
 
@@ -304,13 +308,14 @@ static void *reflection_thread_main(void *arg)
       aimee_log(LOG_INFO, "kb.reflection", "idle=%lds >= threshold=%lds; firing reflection pass",
                 idle_secs, idle_threshold);
       kb_background_set("reflection", "idle=%lds threshold=%lds", idle_secs, idle_threshold);
-      run_reflection_pass(&cfg);
+      run_reflection_pass(cfg);
       kb_background_clear("reflection");
       last_fired = now;
 
       /* Back off until the next idle window */
       sleep((int)(idle_threshold / 2));
    }
+   free(cfg);
    return NULL;
 }
 
@@ -331,7 +336,15 @@ void kb_reflection_init(kb_reflection_ctx_t *ctx)
    }
 
    ctx->stop = 0;
-   if (pthread_create(&ctx->thread, NULL, reflection_thread_main, ctx) == 0)
+   pthread_attr_t attr;
+   pthread_attr_init(&attr);
+   int started = 0;
+   if (pthread_attr_setstacksize(&attr, KB_BG_THREAD_STACK) == 0)
+      started = pthread_create(&ctx->thread, &attr, reflection_thread_main, ctx) == 0;
+   else
+      started = pthread_create(&ctx->thread, NULL, reflection_thread_main, ctx) == 0;
+   pthread_attr_destroy(&attr);
+   if (started)
    {
       ctx->active = 1;
       aimee_log(LOG_INFO, "kb.reflection", "reflection scheduler started");
diff --git a/src/kb/kb_service_workers.c b/src/kb/kb_service_workers.c
index fd9a14d7..1b2053c1 100644
--- a/src/kb/kb_service_workers.c
+++ b/src/kb/kb_service_workers.c
@@ -28,6 +28,8 @@
 #include <pthread.h>
 #include <stdio.h>
 #include <stdlib.h>
+
+#define KB_BG_THREAD_STACK ((size_t)32 * 1024 * 1024)
 #include <string.h>
 #include <time.h>
 #ifndef AIMEE_WINDOWS
@@ -64,9 +66,11 @@ static void *kb_maintenance_timer_thread(void *arg)
 {
    (void)arg;
 
-   config_t cfg;
-   config_load(&cfg);
-   int interval_secs = cfg.kb_maintenance_interval_hours * 3600;
+   config_t *cfg = calloc(1, sizeof(*cfg));
+   if (!cfg)
+      return NULL;
+   config_load(cfg);
+   int interval_secs = cfg->kb_maintenance_interval_hours * 3600;
    if (interval_secs <= 0)
       interval_secs = 86400;
 
@@ -82,16 +86,16 @@ static void *kb_maintenance_timer_thread(void *arg)
          continue;
       elapsed = 0;
 
-      config_load(&cfg);
-      if (!cfg.kb_maintenance_enabled)
+      config_load(cfg);
+      if (!cfg->kb_maintenance_enabled)
          continue;
 
       kb_maintenance_config_t mcfg;
       kb_maintenance_config_defaults(&mcfg);
-      mcfg.lambda = cfg.kb_maintenance_lambda;
-      mcfg.confidence_floor = cfg.kb_maintenance_floor;
-      mcfg.min_age_days = cfg.kb_maintenance_min_age_days;
-      mcfg.orphan_prune_days = cfg.kb_maintenance_orphan_days;
+      mcfg.lambda = cfg->kb_maintenance_lambda;
+      mcfg.confidence_floor = cfg->kb_maintenance_floor;
+      mcfg.min_age_days = cfg->kb_maintenance_min_age_days;
+      mcfg.orphan_prune_days = cfg->kb_maintenance_orphan_days;
       mcfg.dry_run = 0;
 
       kb_background_set("maintenance", "lambda=%.2f floor=%.2f min_age=%dd", mcfg.lambda,
@@ -118,6 +122,7 @@ static void *kb_maintenance_timer_thread(void *arg)
 
       kb_background_clear("maintenance");
    }
+   free(cfg);
    return NULL;
 }
 
@@ -130,16 +135,18 @@ static int g_blob_recon_active = 0;
 static void *kb_blob_recon_timer_thread(void *arg)
 {
    (void)arg;
-   config_t cfg;
+   config_t *cfg = calloc(1, sizeof(*cfg));
+   if (!cfg)
+      return NULL;
    long elapsed = 0;
    while (!g_blob_recon_stop)
    {
       db2_lease_release_idle();
       sleep(1);
       elapsed++;
-      config_load(&cfg);
-      int interval = cfg.kb_pdf_blob_recon_secs;
-      if (!cfg.kb_pdf_assets_enabled || interval <= 0)
+      config_load(cfg);
+      int interval = cfg->kb_pdf_blob_recon_secs;
+      if (!cfg->kb_pdf_assets_enabled || interval <= 0)
       {
          elapsed = 0;
          continue;
@@ -148,7 +155,7 @@ static void *kb_blob_recon_timer_thread(void *arg)
          continue;
       elapsed = 0;
       kb_blob_recon_stats_t st;
-      if (kb_blob_reconcile_run(cfg.kb_pdf_blob_orphan_alarm_mb, KB_BLOB_RECON_GRACE_SECS, &st) ==
+      if (kb_blob_reconcile_run(cfg->kb_pdf_blob_orphan_alarm_mb, KB_BLOB_RECON_GRACE_SECS, &st) ==
           0)
       {
          db2_kb_runtime_state_set_now("last_blob_recon_at");
@@ -157,6 +164,7 @@ static void *kb_blob_recon_timer_thread(void *arg)
          db2_kb_runtime_state_set("last_blob_recon_unlinked", buf);
       }
    }
+   free(cfg);
    return NULL;
 }
 
@@ -265,16 +273,28 @@ int kb_service_init(kb_service_ctx_t *ctx)
    kb_ingest_workers_start(ctx);
 
    g_maintenance_stop = 0;
-   if (pthread_create(&ctx->bg_timer_thread, NULL, kb_maintenance_timer_thread, NULL) == 0)
+   pthread_attr_t mattr;
+   pthread_attr_init(&mattr);
+   if (pthread_attr_setstacksize(&mattr, KB_BG_THREAD_STACK) == 0 &&
+       pthread_create(&ctx->bg_timer_thread, &mattr, kb_maintenance_timer_thread, NULL) == 0)
+      ctx->bg_timer_active = 1;
+   else if (pthread_create(&ctx->bg_timer_thread, NULL, kb_maintenance_timer_thread, NULL) == 0)
       ctx->bg_timer_active = 1;
    else
       aimee_log(LOG_WARN, "kb.service", "failed to start maintenance timer thread");
+   pthread_attr_destroy(&mattr);
 
    g_blob_recon_stop = 0;
-   if (pthread_create(&g_blob_recon_thread, NULL, kb_blob_recon_timer_thread, NULL) == 0)
+   pthread_attr_t battr;
+   pthread_attr_init(&battr);
+   if (pthread_attr_setstacksize(&battr, KB_BG_THREAD_STACK) == 0 &&
+       pthread_create(&g_blob_recon_thread, &battr, kb_blob_recon_timer_thread, NULL) == 0)
+      g_blob_recon_active = 1;
+   else if (pthread_create(&g_blob_recon_thread, NULL, kb_blob_recon_timer_thread, NULL) == 0)
       g_blob_recon_active = 1;
    else
       aimee_log(LOG_WARN, "kb.service", "failed to start blob reconciliation timer thread");
+   pthread_attr_destroy(&battr);
 
    kb_reflection_init(&g_reflection_ctx);
    kb_curator_drain_init(&g_curator_drain_ctx);
diff --git a/src/posix/agent_shell_claude_pty.c b/src/posix/agent_shell_claude_pty.c
index aebebaa6..04a709c3 100644
--- a/src/posix/agent_shell_claude_pty.c
+++ b/src/posix/agent_shell_claude_pty.c
@@ -5,7 +5,12 @@
 #include "agent_shell.h"
 #include "util.h"
 #include <ctype.h>
+#ifdef __APPLE__
+#include <util.h>
+#include <mach-o/dyld.h>
+#else
 #include <pty.h>
+#endif
 #include <signal.h>
 #include <stdio.h>
 #include <stdlib.h>
@@ -15,6 +20,9 @@
 #include <sys/wait.h>
 #include <termios.h>
 #include <unistd.h>
+#ifdef __APPLE__
+int openpty(int *amaster, int *aslave, char *name, struct termios *termp, struct winsize *winp);
+#endif
 
 #define CLAUDE_LINE_MAX (256 * 1024)
 
@@ -22,10 +30,21 @@ static const char *resolve_claude_bin_pty(char *buf, size_t buf_len)
 {
    struct stat st;
    char exe[1024];
+   int have_exe = 0;
+#ifdef __linux__
    ssize_t exe_len = readlink("/proc/self/exe", exe, sizeof(exe) - 1);
    if (exe_len > 0)
    {
       exe[exe_len] = '\0';
+      have_exe = 1;
+   }
+#elif defined(__APPLE__)
+   uint32_t exe_size = sizeof(exe);
+   if (_NSGetExecutablePath(exe, &exe_size) == 0)
+      have_exe = 1;
+#endif
+   if (have_exe)
+   {
       char *slash = strrchr(exe, '/');
       if (slash)
       {
diff --git a/src/posix/agent_tools.c b/src/posix/agent_tools.c
index 996dcaf1..b5639951 100644
--- a/src/posix/agent_tools.c
+++ b/src/posix/agent_tools.c
@@ -739,7 +739,9 @@ char *tool_bash(const char *command, int timeout_ms)
    if (config_load(&cfg) == 0)
       sbox_cfg = cfg.sandbox;
    const char *guard_ro = agent_tools_parent_write_guard_root();
+#ifdef __linux__
    const char *guard_rw = agent_tools_parent_write_guard_write_root();
+#endif
    int guarded_parent = guard_ro && guard_ro[0];
    char guarded_fallback_err[256] = "";
    pid_t pid;
diff --git a/src/posix/platform_ipc.c b/src/posix/platform_ipc.c
index 33a0d742..ab6bd60b 100644
--- a/src/posix/platform_ipc.c
+++ b/src/posix/platform_ipc.c
@@ -13,6 +13,31 @@
 #include <sys/un.h>
 #include <unistd.h>
 
+/* ── macOS compat ────────────────────────────────────────────────────────────
+ * macOS lacks SOCK_CLOEXEC, SOCK_NONBLOCK, and accept4().
+ * Provide drop-in replacements using fcntl. No-op on Linux.
+ */
+#ifdef __APPLE__
+# ifndef SOCK_CLOEXEC
+#   define SOCK_CLOEXEC  0
+# endif
+# ifndef SOCK_NONBLOCK
+#   define SOCK_NONBLOCK 0
+# endif
+static inline int accept4(int sockfd, struct sockaddr *addr,
+                           socklen_t *addrlen, int flags)
+{
+    int fd = accept(sockfd, addr, addrlen);
+    if (fd < 0) return fd;
+    if (flags & SOCK_CLOEXEC)
+        fcntl(fd, F_SETFD, fcntl(fd, F_GETFD, 0) | FD_CLOEXEC);
+    if (flags & SOCK_NONBLOCK)
+        fcntl(fd, F_SETFL, fcntl(fd, F_GETFL, 0) | O_NONBLOCK);
+    return fd;
+}
+#endif /* __APPLE__ */
+
+
 int platform_ipc_listen(const char *path, int backlog)
 {
    /* Ensure parent directory exists */
diff --git a/src/posix/server_compute.c b/src/posix/server_compute.c
index 5f05e685..60592051 100644
--- a/src/posix/server_compute.c
+++ b/src/posix/server_compute.c
@@ -42,8 +42,10 @@ int agent_execute_cli_session(const agent_t *agent, const agent_network_t *netwo
 #include <stdatomic.h>
 #include <time.h>
 #ifdef __linux__
+#ifdef __linux__
 #include <sys/prctl.h>
 #endif
+#endif
 #include <sys/stat.h>
 #include <sys/wait.h>
 #include <unistd.h>
@@ -775,8 +777,8 @@ static void chat_stream_worker_agent(compute_ctx_t *cctx, const char *message, c
 
    agent_result_t result;
    memset(&result, 0, sizeof(result));
-   int rc = agent_run_with_tools(&acfg, "code", system_prompt ? system_prompt : "", message,
-                                 AGENT_DEFAULT_MAX_TOKENS, &result);
+   int rc = agent_run_ex(&acfg, "code", system_prompt ? system_prompt : "", message,
+                         AGENT_DEFAULT_MAX_TOKENS, 0.3, &result);
 
    cli_session_set_error_grace_ms(prev_error_grace);
    cli_session_set_cancel_check(prev_cancel_cb, prev_cancel_ud);
diff --git a/src/server/cli_session_pty.c b/src/server/cli_session_pty.c
index 491abe01..5df221be 100644
--- a/src/server/cli_session_pty.c
+++ b/src/server/cli_session_pty.c
@@ -1,5 +1,7 @@
 /* cli_session_pty.c: server-hosted, PTY-attached CLI sessions (see header). */
-#ifndef _GNU_SOURCE
+#if defined(__APPLE__)
+#define _DARWIN_C_SOURCE
+#elif !defined(_GNU_SOURCE)
 #define _GNU_SOURCE
 #endif
 #include "cli_session_pty.h"
@@ -9,7 +11,11 @@
 #include <fcntl.h>
 #include <poll.h>
 #include <pthread.h>
+#ifdef __APPLE__
+#include <util.h>
+#else
 #include <pty.h>
+#endif
 #include <signal.h>
 #include <stdio.h>
 #include <stdlib.h>
@@ -18,6 +24,10 @@
 #include <sys/wait.h>
 #include <termios.h>
 #include <unistd.h>
+#ifdef __APPLE__
+/* CMake defines _GNU_SOURCE globally, which hides forkpty from util.h on macOS. */
+pid_t forkpty(int *amaster, char *name, struct termios *termp, struct winsize *winp);
+#endif
 
 #define PTY_MAX_SESSIONS 16
 #define PTY_INPUT_CAP    (64 * 1024)
diff --git a/src/server/git_cred_inject.c b/src/server/git_cred_inject.c
index bf2ea033..9ce73d40 100644
--- a/src/server/git_cred_inject.c
+++ b/src/server/git_cred_inject.c
@@ -147,6 +147,7 @@ char **git_cred_inject_build_env_for_repo(const char *principal, const char *rem
    int token_fd = -1;
    if (have_token && fd_mode)
    {
+#if defined(__linux__)
       int mfd = memfd_create("c", MFD_CLOEXEC); /* opaque name: don't advertise a cred in fdinfo */
       size_t tn = strlen(token);
       if (mfd >= 0 && write(mfd, token, tn) == (ssize_t)tn && write(mfd, "\n", 1) == 1)
@@ -157,6 +158,10 @@ char **git_cred_inject_build_env_for_repo(const char *principal, const char *rem
             close(mfd);
          have_token = 0; /* fail closed */
       }
+#else
+      /* macOS/BSD lack memfd — fall back to GH_TOKEN env mode below. */
+      (void)token_fd;
+#endif
    }
 
    /* Start (or reuse) the user's in-memory ssh-agent if they have a vaulted SSH
@@ -168,7 +173,7 @@ char **git_cred_inject_build_env_for_repo(const char *principal, const char *rem
    if (have_token || have_sock)
       envp = build_env(parent_environ, have_token ? token : NULL, forge_cred_askpass_shim(),
                        have_sock ? sock : NULL,
-                       (fd_mode && have_token) ? GIT_CRED_TOKEN_TARGET_FD : -1);
+                       (fd_mode && have_token && token_fd >= 0) ? GIT_CRED_TOKEN_TARGET_FD : -1);
 
    if (!envp && token_fd >= 0) /* build failed → don't leak the memfd */
    {
diff --git a/src/server/git_ssh_agent.c b/src/server/git_ssh_agent.c
index ed6bb515..66a0eb70 100644
--- a/src/server/git_ssh_agent.c
+++ b/src/server/git_ssh_agent.c
@@ -10,7 +10,9 @@
 #include <stdlib.h>
 #include <string.h>
 #include <sys/mman.h>
+#ifdef __linux__
 #include <sys/prctl.h>
+#endif
 #include <sys/resource.h>
 #include <sys/stat.h>
 #include <sys/wait.h>
@@ -44,7 +46,9 @@ static int run_child(const char *const argv[], char *const envp[], int keyfd, ch
    {
       struct rlimit z = {0, 0};
       setrlimit(RLIMIT_CORE, &z);
+#ifdef __linux__
       prctl(PR_SET_DUMPABLE, 0, 0, 0, 0);
+#endif
       if (keyfd >= 0)
       {
          if (keyfd != 3)
@@ -57,7 +61,11 @@ static int run_child(const char *const argv[], char *const envp[], int keyfd, ch
          close(pipefd[0]);
          close(pipefd[1]);
       }
+#ifdef __linux__
       execvpe(argv[0], (char *const *)argv, envp);
+#else
+      execve(argv[0], (char *const *)argv, envp ? envp : environ);
+#endif
       _exit(127);
    }
    if (pipefd[1] >= 0)
@@ -183,8 +191,9 @@ int git_ssh_agent_ensure(const char *principal, char *out, size_t cap)
     * so the parent's fd can't leak to an unrelated concurrent fork+exec; the
     * ssh-add child still gets it because run_child dup2()'s it onto fd 3, which
     * is created WITHOUT cloexec (and run_child clears it explicitly too). */
-   int mfd = memfd_create("aimee-sshkey", MFD_CLOEXEC);
    int ok = 0;
+#if defined(__linux__)
+   int mfd = memfd_create("aimee-sshkey", MFD_CLOEXEC);
    if (mfd >= 0)
    {
       /* ssh-add refuses a key whose file looks world-accessible; a memfd is
@@ -205,6 +214,11 @@ int git_ssh_agent_ensure(const char *principal, char *out, size_t cap)
       }
       close(mfd);
    }
+#else
+   memset(key, 0, sizeof(key));
+   git_ssh_agent_stop(principal);
+   return -1; /* memfd ssh-key staging is Linux-only */
+#endif
    memset(key, 0, sizeof(key));
 
    if (!ok)
diff --git a/src/server/script_runner.c b/src/server/script_runner.c
index de517a44..f87569cf 100644
--- a/src/server/script_runner.c
+++ b/src/server/script_runner.c
@@ -332,12 +332,20 @@ char *tool_execute_script(const char *language, const char *body, int timeout_se
       if (strcmp(language, "python") == 0)
       {
          char *const argv[] = {"python3", "-c", (char *)body, NULL};
+#ifdef __linux__
          execvpe("python3", argv, envp);
+#else
+         execve("python3", argv, envp ? envp : environ);
+#endif
       }
       else
       {
          char *const argv[] = {"bash", "-c", (char *)body, NULL};
+#ifdef __linux__
          execvpe("bash", argv, envp);
+#else
+         execve("bash", argv, envp ? envp : environ);
+#endif
       }
       _exit(127);
    }
diff --git a/src/server/trigger_scheduler.c b/src/server/trigger_scheduler.c
index 04042b62..a05ad7fa 100644
--- a/src/server/trigger_scheduler.c
+++ b/src/server/trigger_scheduler.c
@@ -525,18 +525,22 @@ static int config_has_cron_job(const config_t *cfg, const char *id)
 /* Scheduler tick                                                      */
 /* ------------------------------------------------------------------ */
 
+#define SCHED_THREAD_STACK ((size_t)32 * 1024 * 1024)
+
 static void sched_tick(void)
 {
-   config_t cfg;
-   config_load(&cfg);
+   config_t *cfg = calloc(1, sizeof(*cfg));
+   if (!cfg)
+      return;
+   config_load(cfg);
 
    time_t now = time(NULL);
    struct tm tm;
    localtime_r(&now, &tm);
 
-   for (int i = 0; i < cfg.trigger_rule_count && i < TRIGGER_RULES_MAX; i++)
+   for (int i = 0; i < cfg->trigger_rule_count && i < TRIGGER_RULES_MAX; i++)
    {
-      const trigger_rule_t *rule = &cfg.trigger_rules[i];
+      const trigger_rule_t *rule = &cfg->trigger_rules[i];
 
       if (strcmp(rule->source, "cron") != 0)
          continue;
@@ -555,9 +559,9 @@ static void sched_tick(void)
       trigger_scheduler_fire_rule(rule);
    }
 
-   for (int i = 0; i < cfg.cron_job_count && i < CRON_JOBS_MAX; i++)
+   for (int i = 0; i < cfg->cron_job_count && i < CRON_JOBS_MAX; i++)
    {
-      const cron_job_t *job = &cfg.cron_jobs[i];
+      const cron_job_t *job = &cfg->cron_jobs[i];
       if (!job->enabled || !job->id[0] || !job->schedule[0])
          continue;
       int match = schedule_matches(job->schedule, &tm);
@@ -576,7 +580,7 @@ static void sched_tick(void)
    for (int i = 0; i < db_count && i < CRON_JOBS_MAX; i++)
    {
       const cron_job_t *job = &db_jobs[i];
-      if (config_has_cron_job(&cfg, job->id) || !job->enabled || !job->id[0] || !job->schedule[0])
+      if (config_has_cron_job(cfg, job->id) || !job->enabled || !job->id[0] || !job->schedule[0])
          continue;
       int match = schedule_matches(job->schedule, &tm);
       if (match != 1)
@@ -590,8 +594,9 @@ static void sched_tick(void)
    }
 
    /* Skill curator: idle-guarded; safe to call on every tick. */
-   if (cfg.skills_curator_enabled)
+   if (cfg->skills_curator_enabled)
       skill_curator_maybe();
+   free(cfg);
 }
 
 /* ------------------------------------------------------------------ */
@@ -630,7 +635,13 @@ void trigger_scheduler_init(void)
    memset(g_last_cron_job_fired, 0, sizeof(g_last_cron_job_fired));
    memset(g_last_db_cron_job_fired, 0, sizeof(g_last_db_cron_job_fired));
    g_sched_stop = 0;
-   pthread_create(&g_sched_thread, NULL, sched_thread_fn, NULL);
+   pthread_attr_t attr;
+   pthread_attr_init(&attr);
+   if (pthread_attr_setstacksize(&attr, SCHED_THREAD_STACK) == 0)
+      pthread_create(&g_sched_thread, &attr, sched_thread_fn, NULL);
+   else
+      pthread_create(&g_sched_thread, NULL, sched_thread_fn, NULL);
+   pthread_attr_destroy(&attr);
 }
 
 void trigger_scheduler_shutdown(void)
diff --git a/src/server/webuser_editor.c b/src/server/webuser_editor.c
index d2dea46d..ccb1bf81 100644
--- a/src/server/webuser_editor.c
+++ b/src/server/webuser_editor.c
@@ -16,7 +16,9 @@
 #include <stdio.h>
 #include <stdlib.h>
 #include <string.h>
+#ifdef __linux__
 #include <sys/prctl.h>
+#endif
 #include <sys/resource.h>
 #include <sys/socket.h>
 #include <sys/stat.h>
@@ -313,8 +315,10 @@ static pid_t spawn_code_server(const char *principal, const char *userroot, int
       setsid();
       struct rlimit z = {0, 0};
       setrlimit(RLIMIT_CORE, &z);
+#ifdef __linux__
       prctl(PR_SET_DUMPABLE, 0, 0, 0, 0);
       prctl(PR_SET_NO_NEW_PRIVS, 1, 0, 0, 0);
+#endif
       int devnull = open("/dev/null", O_RDWR);
       if (devnull >= 0)
       {
@@ -332,7 +336,11 @@ static pid_t spawn_code_server(const char *principal, const char *userroot, int
       }
       if (chdir(userroot) != 0)
          _exit(127);
+#ifdef __linux__
       execvpe(bin, (char *const *)argv, envp);
+#else
+      execve(bin, (char *const *)argv, envp ? envp : environ);
+#endif
       _exit(127);
    }
 
diff --git a/src/server/webuser_runtime.c b/src/server/webuser_runtime.c
index e45df134..8c6e7489 100644
--- a/src/server/webuser_runtime.c
+++ b/src/server/webuser_runtime.c
@@ -10,7 +10,11 @@
 #include <stdlib.h>
 #include <string.h>
 #include <sys/stat.h>
+#ifdef __APPLE__
+#include <sys/mount.h>
+#else
 #include <sys/vfs.h>
+#endif
 #include <unistd.h>
 
 /* From linux/magic.h — declared here so the TU needs no kernel headers. */
@@ -30,9 +34,13 @@ int webuser_runtime_is_tmpfs(const char *path)
    struct statfs sf;
    if (statfs(path, &sf) != 0)
       return -1;
+#ifdef __APPLE__
+   return (strcmp(sf.f_fstypename, "tmpfs") == 0) ? 1 : 0;
+#else
    if ((unsigned long)sf.f_type == TMPFS_MAGIC || (unsigned long)sf.f_type == RAMFS_MAGIC)
       return 1;
    return 0;
+#endif
 }
 
 /* Validated <name> from a "webuser:<name>" principal, or -1. */
