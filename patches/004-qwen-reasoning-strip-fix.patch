diff --git a/src/server/agent_bridge.c b/src/server/agent_bridge.c
index 8bf7302e..73d00cbd 100644
--- a/src/server/agent_bridge.c
+++ b/src/server/agent_bridge.c
@@ -227,17 +227,32 @@ cJSON *agent_build_request_anthropic(const agent_t *agent, cJSON *messages, cJSO
 
 /* --- Response parsers --- */
 
+#define REDACTED_THINKING_OPEN  "<think>"
+#define REDACTED_THINKING_CLOSE "</think>"
+
 /* Strip <think>...</think> blocks from a model response.
  * Qwen3 and similar reasoning models embed thinking in the content field when
  * the llama.cpp server is not configured to separate it into reasoning_content.
- * Returns a new malloc'd string; caller must free. Returns NULL on alloc failure. */
+ * When only a closing tag is present, keep the text after it (the user-visible
+ * answer). Returns a new malloc'd string; caller must free. Returns NULL on
+ * alloc failure. */
 static char *strip_thinking_blocks(const char *text)
 {
    if (!text)
       return NULL;
-   if (!strstr(text, "<think>") && !strstr(text, "</think>"))
+   if (!strstr(text, REDACTED_THINKING_OPEN) && !strstr(text, REDACTED_THINKING_CLOSE))
       return strdup(text);
 
+   const char *close = strstr(text, REDACTED_THINKING_CLOSE);
+   if (close)
+   {
+      const char *after = close + (sizeof(REDACTED_THINKING_CLOSE) - 1);
+      while (*after == '\n' || *after == '\r' || *after == ' ' || *after == '\t')
+         after++;
+      if (*after)
+         return strdup(after);
+   }
+
    size_t len = strlen(text);
    char *out = malloc(len + 1);
    if (!out)
@@ -247,20 +262,20 @@ static char *strip_thinking_blocks(const char *text)
    const char *p = text;
    while (*p)
    {
-      if (strncmp(p, "<think>", 7) == 0)
+      if (strncmp(p, REDACTED_THINKING_OPEN, sizeof(REDACTED_THINKING_OPEN) - 1) == 0)
       {
-         const char *end = strstr(p + 7, "</think>");
+         const char *end = strstr(p + (sizeof(REDACTED_THINKING_OPEN) - 1), REDACTED_THINKING_CLOSE);
          if (end)
          {
-            p = end + 8;
+            p = end + (sizeof(REDACTED_THINKING_CLOSE) - 1);
             while (*p == '\n' || *p == '\r' || *p == ' ' || *p == '\t')
                p++;
             continue;
          }
       }
-      if (strncmp(p, "</think>", 8) == 0)
+      if (strncmp(p, REDACTED_THINKING_CLOSE, sizeof(REDACTED_THINKING_CLOSE) - 1) == 0)
       {
-         p += 8;
+         p += sizeof(REDACTED_THINKING_CLOSE) - 1;
          while (*p == '\n' || *p == '\r' || *p == ' ' || *p == '\t')
             p++;
          continue;
@@ -440,23 +455,33 @@ void agent_parse_response_openai(cJSON *root, parsed_response_t *out)
 
       /* Strip <think>...</think> blocks that Qwen3/reasoning models embed in
        * content when the server doesn't separate them into reasoning_content. */
-      if (out->content && (strstr(out->content, "<think>") || strstr(out->content, "</think>")))
+      if (out->content && (strstr(out->content, REDACTED_THINKING_OPEN) ||
+                           strstr(out->content, REDACTED_THINKING_CLOSE)))
       {
-         char *stripped = strip_thinking_blocks(out->content);
-         if (stripped)
+         char *raw = out->content;
+         char *stripped = strip_thinking_blocks(raw);
+         if (stripped && stripped[0])
          {
             free(out->content);
-            out->content = stripped[0] ? stripped : (free(stripped), NULL);
+            out->content = stripped;
          }
+         else
+            free(stripped);
       }
       if (out->content)
       {
-         char *stripped = strip_llm_private_scaffold(out->content);
-         if (stripped)
+         char *raw = out->content;
+         char *stripped = strip_llm_private_scaffold(raw);
+         if (stripped && stripped[0])
          {
             free(out->content);
             out->content = stripped;
          }
+         else
+         {
+            free(stripped);
+            out->content = raw;
+         }
       }
 
       /* Compatibility: some llama.cpp builds return finish_reason="stop" even
diff --git a/src/util.c b/src/util.c
index 5645cece..82faccc7 100644
--- a/src/util.c
+++ b/src/util.c
@@ -56,10 +56,10 @@ char *strip_llm_private_scaffold(const char *text)
    if (!marker)
       marker = find_ci_local(text, "Final Answer");
    if (!marker)
-      return safe_strdup("");
+      return safe_strdup(text ? text : "");
    p = strchr(marker, '\n');
    if (!p)
-      return safe_strdup("");
+      return safe_strdup(text ? text : "");
    p++;
    while (*p == '\n' || *p == '\r' || *p == ' ' || *p == '\t' || *p == '*' || *p == '-' ||
           *p == '"' || *p == '\'')
