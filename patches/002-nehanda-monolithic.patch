diff --git a/src/db1/db_schema.c b/src/db1/db_schema.c
index 0000000..1111111 100644
--- a/src/db1/db_schema.c
+++ b/src/db1/db_schema.c
@@ -8,6 +8,9 @@
 #include "../schema_data.h"
 #include <sqlite3.h>
 #include <stdio.h>
 #include <string.h>
+#ifdef NEHANDA_BUILD
+#include "nehanda_unified_storage.h"
+#endif
 
 static void copy_err(char *errbuf, size_t errlen, const char *src)
 {
@@ -77,6 +80,11 @@ int db1_apply_schema_sqlite(sqlite3 *db, char *errbuf, size_t errlen)
       sqlite3_free(err);
       return -1;
    }
+#ifdef NEHANDA_BUILD
+   if (nehanda_apply_unified_schema(db) != 0)
+      return -1;
+#endif
    return 0;
 }
 
diff --git a/src/acp_server.c b/src/acp_server.c
index 0000000..2222222 100644
--- a/src/acp_server.c
+++ b/src/acp_server.c
@@ -5,6 +5,10 @@
  * (one message per line); logs/diagnostics belong on stderr. */
 #include "acp_server.h"
 #include "cJSON.h"
+#ifdef NEHANDA_BUILD
+#include "nehanda_inprocess.h"
+#include <sqlite3.h>
+#endif
 
 #include <stdio.h>
 #include <stdlib.h>
@@ -382,6 +386,14 @@ int acp_server_run(acp_prompt_dispatch_fn dispatch)
             fflush(stdout);
             continue;
          }
+#ifdef NEHANDA_BUILD
+         {
+            sqlite3 *udb = nehanda_unified_db();
+            ToolTurnContext ctx = {session[0] ? session : "", content, 0};
+            handle_incoming_tool_invocation(&ctx, udb);
+         }
+#endif
          char *reply = dispatch ? dispatch(content, session[0] ? session : NULL) : NULL;
          if (reply)
          {
diff --git a/src/db1/server_sessions.c b/src/db1/server_sessions.c
index 0000000..3333333 100644
--- a/src/db1/server_sessions.c
+++ b/src/db1/server_sessions.c
@@ -7,6 +7,9 @@
 #include <stddef.h>
 #include <stdio.h>
 #include <string.h>
+#ifdef NEHANDA_BUILD
+#include "nehanda_token.h"
+#endif
 
 static void fill_row(db1_server_session_t *out, sqlite3_stmt *stmt)
 {
